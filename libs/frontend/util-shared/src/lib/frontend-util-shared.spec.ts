import { frontendUtilShared } from './frontend-util-shared';

describe('frontendUtilShared', () => {
  it('should work', () => {
    expect(frontendUtilShared()).toEqual('frontend-util-shared');
  });
});
